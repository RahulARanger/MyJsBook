// if we see this, python by now raised TypeError

console.log("6" * "10" + "9"); // js tries to convert string to number while * this is type corection
console.log(68 + "1"); // concatenation
// this thing is done by js automatically
