let sample = 69;

sample += 1;
sample -= 1;

// we can do this, same as python

const sample_ = "sample";
// sample_ = false; // we can assign values for the second time for the const variables

please = come = inside = me = 69;
console.log(please, come, inside, me);
