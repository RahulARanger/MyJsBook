"use strict"; // must be at the first line

const interface = 69; // can't use this reserved words now

sample = true; // this was possible in js
// but in strict mode, we need to declare the variable before defining it
